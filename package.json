{
  "name": "electro-cell",
  "version": "1.0.0",
  "description": "A cellular automat written in electron typescript.",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "lint": "eslint -c .eslintrc --ext .ts ./src",
    "test": "npm run build && electron ./dist/main.js --test --dev-tools",
    "start": "npm run build && electron ./dist/main.js"
  },
  "repository": "https://github.com/thomasvolk/electro-cell",
  "keywords": [
    "Electron",
    "typescript"
  ],
  "author": "Thomas Volk",
  "license": "Apache-2.0",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^3.4.0",
    "@typescript-eslint/parser": "^3.4.0",
    "electron": "^11.3.0",
    "eslint": "^7.3.1",
    "typescript": "^3.9.5"
  }
}
